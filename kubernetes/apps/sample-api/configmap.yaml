apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-api-config
  namespace: app
  labels:
    app: sample-api
    team: platform
data:
  APP_ENV: "dev"
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"
  SERVER_PORT: "8080"
  METRICS_PORT: "9090"
  DB_PORT: "5432"
  DB_NAME: "app"
